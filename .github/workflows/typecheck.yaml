name: Type Checking
on: [push]
jobs:
  type-check:
    name: 👨‍⚕️ Type Checking
    runs-on: ubuntu-latest
    steps:
      - name: 🔔 Check out
        uses: actions/checkout@v3

      - name: 🏗️ python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: ⬇️ Python PDM
        uses: pdm-project/setup-pdm@v4
        with:
          python-version: "3.12"
          cache: true

      - name: ⬇️ Python Dependencies
        run: pdm sync -G:all

      - name: 🏃 mypy
        run: |
          pdm run mypy src --install-types --non-interactive
          pdm run mypy src
